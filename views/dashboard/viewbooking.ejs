<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Table Design</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            text-align: left;
            padding: 12px;
            border: 1px solid #dee2e6;
        }

        th {
            background-color: #343a40;
            color: #fff;
        }

        tr:nth-child(even) {
            background-color: #f8f9fa;
        }

        tr:hover {
            background-color: #f1f3f5;
        }
    </style>
</head>
<body>
<%- include("./header.ejs"); %>
<div class="container-fluid mt-4">
    <div class="row">
        <!-- Sidebar -->
        <div class="col-md-3">
            <%- include("./left.ejs"); %>
        </div>
        <!-- Main Content -->
        <div class="col-md-9">
            <h3 class="mb-4">Bookings</h3>
            <% if(bookdata.length>0){ %>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Phone Number</th>
                        <th>Email</th>
                        <th>Persons</th>
                        <th>Date</th>
                    </tr>
                </thead>

                <tbody>
                    <% for(var i=0; i<bookdata.length; i++){%>
                        <tr>
                            <td><%= bookdata[i].name %></td>
                            <td><%= bookdata[i].phone %></td>
                            <td><%= bookdata[i].email %></td>
                            <td><%= bookdata[i].persons %></td>
                            <td><%= bookdata[i].date %></td>
                            
                            <td><a href="/edit3/<%= bookdata[i]._id %>"><button class="btn btn-outline-light btn-sm">edit</button></a></td>
                            <td><a href="/delete3/<%= bookdata[i]._id %>"><button class="btn btn-outline-light btn-sm">delete</button></a></td>
                        </tr>
                        <% } %>
                </tbody>
                
            </table>

            <% } else { %>
                No record found
           <%} %>

        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-csIQvPFP30BnmaFB9hIBDW28wwOkbj3BSuwxjoYeFcrHnBKTc7f+E3w0LKxHg93z" crossorigin="anonymous"></script>
</body>
</html>
