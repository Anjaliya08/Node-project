<!DOCTYPE html>
<html>
<head>
    <title>book Page</title>
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<style>
  .b1 {
  font-family: Arial, sans-serif;
  background-color: #f9f9f9;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  padding: 20px;
}

.c2 {
  display: flex;
  gap: 20px;
  background: #fff;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
  border-radius: 8px;
  overflow: hidden;
}

.form-section, .map-section {
  padding: 20px;
}

.form-section {
  flex: 1;
}

.head1 {
  font-size: 24px;
  margin-bottom: 20px;
  color: #333;
}

form {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

form input, form button {
  padding: 10px;
  font-size: 16px;
  border: 1px solid #ddd;
  border-radius: 4px;
}

form input:focus {
  border-color: #ff9800;
  outline: none;
}

form button {
  background-color: #ff9800;
  color: #fff;
  border: none;
  cursor: pointer;
}

form button:hover {
  background-color: #e68a00;
}

.map-section {
  flex: 1;
  position: relative;
}
iframe {
  width: 100%;
  height: 300px;
  border: none;
}

.map-error {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: rgba(0, 0, 0, 0.7);
  color: #fff;
  padding: 10px;
  border-radius: 4px;
  font-size: 14px;
  text-align: center;
}
</style>
<body>
    
<div class="container c2 b1">
    <div class="form-section">
      <h1 class="head1">Book A Table</h1>
      <form method="post" >
        <input type="text" name="name" value="<%= bookdata.name %>" placeholder="Your Name" required>
        <input type="text" name="phone" value="<%= bookdata.phone %>"  placeholder="Phone Number" required>
        <input type="email" name="email" value="<%= bookdata.email %>"  placeholder="Your Email" required>
        <input type="number" name="persons" value="<%= bookdata.persons %>"  placeholder="How many persons?" required>
        <input type="date" name="date" value="<%= bookdata.date %>"  required>
        <button type="submit">Book Now</button>
      </form>
    </div>
  </div>
  
</body>
</html>